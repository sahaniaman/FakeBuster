# FakeBuster Backend - Essential Requirements Only
# This simplified version avoids compilation issues on Windows

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# HTTP and async
aiohttp>=3.9.0
httpx>=0.25.0
aiofiles>=23.0.0

# Firebase Integration
firebase-admin>=6.2.0

# Authentication (optional for now)
python-jose[cryptography]>=3.3.0

# Machine Learning
scikit-learn>=1.3.0
pandas>=2.1.0
numpy>=1.24.0
joblib>=1.3.0
xgboost>=2.0.0

# Environment Variables
python-dotenv>=1.0.0

# Basic Text Processing (no compilation needed)
beautifulsoup4>=4.12.2
requests>=2.31.0

# Caching and Rate Limiting
cachetools>=5.3.0
redis>=4.6.0  # Optional, fallback to memory cache if not available

# Security Features
validators>=0.20.0  # URL validation
pycountry>=22.3.5  # Country code lookup
tldextract>=3.4.0  # Domain extraction and analysis

# Note: ML packages (scikit-learn, pandas, numpy) can be installed later
# after setting up Microsoft Visual C++ Build Tools if needed
# For now, the API will work without ML model functionality
